{
	"private": true,
	"version": "0.0.0",
	"scripts": {
		"dev": "wrangler dev --env dev",
		"deploy": "wrangler publish --env prod",
		"tunnel": "cloudflared tunnel -n sleme-dev --url localhost:8787 --ui",
		"build": "wrangler publish --env dev --dry-run --outdir=dist"
	},
	"devDependencies": {
		"wrangler": "^2.0.25"
	},
	"volta": {
		"node": "16.16.0",
		"yarn": "1.22.19"
	}
}
